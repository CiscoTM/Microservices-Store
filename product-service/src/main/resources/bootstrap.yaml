spring:
  application:
    name: product-service
  cloud:
    config:
      username: root
      password: s3cr3t
  config:
    import: "optional:configserver:http://localhost:8081"
  boot:
    admin:
      client:
        url: http://localhost:8086


